function allVoices(){return speechSynthesis.getVoices?.()||[];}
function pickVoice(locale='en-US',gender){let vs=allVoices().filter(v=>v.lang?.toLowerCase().startsWith(locale.toLowerCase().slice(0,2)));const exact=vs.filter(v=>v.lang?.toLowerCase()===locale.toLowerCase());if(exact.length)vs=exact;const female=/(samantha|victoria|karen|zira|ava|female|susan|emma)/i;const male=/(daniel|alex|fred|aaron|male|tom|george)/i;if(gender==='female'){const x=vs.find(v=>female.test(v.name));if(x)return x;}if(gender==='male'){const x=vs.find(v=>male.test(v.name));if(x)return x;}return vs[0]||allVoices()[0]||null;}
export function stopAudio(){if('speechSynthesis' in window) speechSynthesis.cancel();}
export async function speak(text,{locale='en-US',rate=.95,gender='female'}={}){if(!('speechSynthesis' in window)) throw new Error('Synthèse vocale indisponible');stopAudio();return new Promise((resolve,reject)=>{const u=new SpeechSynthesisUtterance(text);u.lang=locale;u.rate=rate;u.voice=pickVoice(locale,gender);u.onend=resolve;u.onerror=e=>reject(e.error||e);speechSynthesis.speak(u);});}
export async function playQuestionAudio(q){if(q.audio?.mode==='remote'&&q.audio.url){const a=new Audio(q.audio.url);await a.play();return;}if(q.audioUrl){const a=new Audio(q.audioUrl);await a.play();return;}const script=Array.isArray(q.audioScript)?q.audioScript:[];for(const item of script){await speak(item.text,{locale:item.locale||'en-US',rate:item.rate||.95,gender:item.gender||'female'});}}
export function audioCapabilities(){return {speechSynthesis:'speechSynthesis' in window,voiceCount:allVoices().length,voices:allVoices().slice(0,8).map(v=>({name:v.name,lang:v.lang,local:v.localService}))};}
