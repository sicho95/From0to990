# @Sites backend minimal — From0to990

Le frontend est déjà hébergé sur GitHub Pages. Ne pas le recréer dans @Sites.

## Rôle de @Sites

Uniquement :
1. exposer une API HTTP sous `/api/v1/*` ;
2. utiliser D1 avec `schema.sql` ;
3. autoriser CORS uniquement depuis `https://sicho95.github.io` ;
4. implémenter le protocole `openapi.yaml` ;
5. fournir export/import administrateur complets et portables.
